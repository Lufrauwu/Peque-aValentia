using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonLand : MonoBehaviour
{
    public AudioSource buttonland;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void ButtonLandingSound()
    {
        buttonland.Play();
    }
}

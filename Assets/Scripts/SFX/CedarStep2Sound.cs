using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CedarStep2Sound : MonoBehaviour
{
    public AudioSource cedarsteps;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void CedarRunSound()
    {
        cedarsteps.Play();
    }
}
